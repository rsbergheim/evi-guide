import React from 'react';

interface KursProps {
    title: string;
}

const Kurs = () => {
    return (
        <div>
            <h1>Kurs</h1>
            {/* Add more content for the sub page as needed */}
        </div>
    );
}

export default Kurs;
