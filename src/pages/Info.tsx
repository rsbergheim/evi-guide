import React from 'react';

interface InfoProps {
    title: string;
}

const Info = () => {
    return (
        <div>
            <h1>Info</h1>
            {/* Add more content for the sub page as needed */}
        </div>
    );
}

export default Info;
